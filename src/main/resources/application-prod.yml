spring:
  datasource:
    url: jdbc:postgresql://postgres:cyuIExXxPIQVpYYiVzcYzQSGYnEhAuem@autorack.proxy.rlwy.net:23091/railway

    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create # ou none, validate, create, create-drop conforme necessário
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect